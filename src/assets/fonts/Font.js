// styles/fonts.js
import { StyleSheet } from 'react-native';

const fonts = StyleSheet.create({
    textTitle16: {
        fontSize: 16,
        fontWeight: '600',

    },
    textDescription14: {
        fontSize: 14,
        fontWeight: '400',
        color: 'darkgary'

    },
});

export default fonts;
